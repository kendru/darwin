# Europa | An experimental web-native DAW

## Getting Started

Start an HTTP server by running `./script/server.sh`. This will start a web server that serves the `public` directory on port `8080`.

The index page loads a `main.js` file, which boots the application by loading an audio
